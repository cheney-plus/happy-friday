
文档性质：基础框架搭建指引，面向 AI Coding Agent
技术栈：Tauri 2.x + Vue 3 + Rust
目标：搭建可扩展的项目骨架，而非实现业务功能

一、整体架构约束
1.1 三层分离原则
整个应用严格划分为三个层次，每一层有明确的职责边界，禁止跨层直接调用：

Rust 后端层：负责文件 IO、数据库操作、系统 API 调用、窗口管理等所有与系统交互的逻辑。通过 Tauri Command 暴露接口，绝不包含任何 UI 逻辑。
Vue 前端层：负责全部 UI 渲染、用户交互、路由切换、状态管理。通过 @tauri-apps/api 调用 Rust 后端，绝不直接操作文件系统或数据库。
桥接层：Tauri 提供的 IPC 通信机制。所有跨层调用必须通过定义好的 Command 和 Event，禁止使用 eval 或动态注入脚本。

1.2 对 AI Agent 的核心要求
先搭骨架，再填血肉：本阶段只建立页面路由、导航框架、国际化基础设施、状态管理结构、Rust 模块划分。每个页面只需要放置占位内容，不需要实现任何业务逻辑。
不要过度设计：不要在本阶段引入富文本编辑器、数据库 ORM、复杂动画等非框架必需的依赖。
不要遗漏占位：每一个声明的页面和路由，都必须有对应的 Vue 组件文件，哪怕里面只有一行文字标识这是什么页面。

二、菜单功能模块与页面规划
2.1 导航布局方案
采用 左侧固定侧边栏 + 右侧内容区 的经典布局：

text

┌─────┬─────────────────────────────┐
│ 头像 │         内容区               │
│     │                             │
│ 模块 │    <router-view />          │
│ 模块 │                             │
│ 模块 │                             │
│     │                             │
│     │                             │
│ 历史 │                             │
│ 设置 │                             │
└─────┴─────────────────────────────┘
侧边栏采用极简模式，宽度固定为 64px，仅展示图标（分上下两组排列）。

2.2 六大功能模块明细
每个模块对应一个一级路由，模块内部的路由作为二级路由嵌套在对应模块下。

模块
路由路径
说明
包含的子页面/视图
用户	/user	用户信息与账户管理	用户资料页（默认）、暂时不实现登录/注册页（未登录时展示）
知识库	/workspace	知识库的创建与管理，类似 Notion 的 Workspace 概念	知识库列表页（默认）、知识库详情页（/workspace/:id）
笔记	/note	笔记的编辑与组织，核心功能模块	笔记列表页（默认）、笔记编辑页（/note/:id）、快速新建页
日程	/schedule	日程管理与提醒	日历视图页（默认）、日程列表页、日程详情页（/schedule/:id）
历史	/history	操作记录与版本历史	历史列表页（默认）、版本对比页（/history/:id）
设置	/settings	应用全局配置	通用设置页（默认）、外观设置页、快捷键设置页、关于页
你好周五	/friday	 AI对话框页    与 AI 交互的对话框，用户可以输入问题、接收回答。



2.3 默认路由与入口
应用启动后的默认路由：/workspace（知识库模块）
404 情况：重定向到 /workspace，不展示独立 404 页面 
2.4 扩展性设计：模块注册制
这是本框架最重要的设计决策之一。

侧边栏菜单不允许硬编码。必须采用「模块注册表」模式：

在前端维护一个统一的菜单配置数组（或对象），每个菜单项是一个结构化的配置对象，包含：key（唯一标识）、path（路由路径）、icon（图标组件引用）、i18nKey（国际化键名）、requireAuth（是否需要登录）、order（排序权重）、children（子菜单数组，可选）。
侧边栏组件只负责读取这个配置数组并渲染，不包含任何具体的模块名或路径。
后续新增模块时，只需要：1）创建对应的 Vue 页面组件；2）在路由配置中注册；3）在菜单配置数组中追加一条记录。不需要修改侧边栏组件本身。
Rust 后端同理：每个模块对应一个独立的 Rust 模块文件，通过一个统一的入口模块（如 commands/mod.rs）集中 re-export，Tauri 的 invoke_handler 从这个入口模块获取所有命令。新增模块时只需在入口模块中添加一行 mod 和对应的注册。

三、国际化（i18n）方案
3.1 技术选型
使用 vue-i18n，这是 Vue 生态最成熟的国际化方案。

3.2 语言支持
仅支持两种语言：

语言代码
语言
说明
zh-CN	简体中文	默认语言
en-US	English	备选语言

不要预留其他语言的占位文件或配置。如果后续需要增加语言，那是另一个独立的扩展任务。

3.3 语言文件组织方式
采用按模块拆分而非单文件方式：

每个功能模块对应一个独立的语言文件（如 user.json、workspace.json、note.json 等）
一个 common.json 存放跨模块复用的通用文案（如"确认"、"取消"、"保存"、"删除"等）
一个 index.ts 入口文件负责合并所有模块语言文件并导出
中英文文件的键名必须完全一一对应，不允许任何一方存在孤立键

3.4 语言切换机制
用户选择的语言存储在 Rust 后端的持久化配置中（本阶段先用简单的 JSON 配置文件，不要引入数据库）
应用启动时，Rust 读取语言配置并通过 Tauri Event 传递给前端，前端完成 vue-i18n 的初始化
用户在设置中切换语言时，前端先切换（即时生效），同时通知 Rust 后端持久化
禁止在 localStorage 中存储语言偏好，所有持久化数据统一由 Rust 后端管理

3.5 使用规范
所有用户可见的文案必须使用 t('key') 方式引用，禁止在模板或 JS 中硬编码任何中文字符串或英文字符串
唯一例外：开发阶段的占位文字可以临时硬编码，但必须以 <!-- TODO: i18n --> 注释标记
i18n key 的命名规范：模块名.子模块名.文案用途，如 workspace.list.createButton、common.action.confirm

四、状态管理
4.1 技术选型
使用 Pinia，Vue 3 官方推荐的状态管理方案。

4.2 Store 划分
按功能模块划分 Store，每个模块一个 Store 文件：

Store 名称
职责
useAppStore	  全局应用状态：侧边栏折叠状态、当前语言、主题模式（亮/暗）、加载状态
useUserStore	用户状态：登录状态、用户信息、token
useWorkspaceStore	知识库状态：知识库列表、当前选中的知识库
useNoteStore	笔记状态：笔记列表、当前编辑的笔记、草稿状态
useScheduleStore	日程状态：日程列表、当前日期
useHistoryStore	历史状态：历史记录列表、筛选条件
useSettingsStore	设置状态：各设置项的临时编辑值（未保存时与持久化值分离）

4.3 对 AI Agent 的约束
本阶段每个 Store 不需要实现 ， 实现在后续业务开发时按需补充。 
Store 之间禁止直接互相引用。如果存在跨 Store 的数据依赖，通过 useAppStore 中转或在前端组件层组合。
不要在本阶段引入任何数据持久化插件（如 pinia-plugin-persistedstate）。持久化由 Rust 后端负责，前端 Store 的数据在应用启动时从 Rust 获取并填充。
五、Rust 后端模块划分
5.1 模块结构规划
Rust 后端按职责划分为以下模块：

模块
职责
config	应用配置的读写（语言、主题、窗口位置等），配置文件使用 JSON 格式存储在系统应用数据目录下
commands	所有 Tauri Command 的统一入口，内部按业务模块分子模块（user、workspace、note、schedule、history、settings）
events	定义所有 Tauri Event 的常量名称，集中管理避免字符串硬编码
error	统一的错误类型定义，所有 Command 返回的错误必须转换为这个统一类型，确保前端能稳定解析
types	跨前后端共享的数据结构定义（对应前端 TypeScript 的 interface/type）

5.2 对 AI Agent 的约束
本阶段每个 Command 函数只需要有函数签名和返回类型，函数体可以只返回一个空值或占位数据。目的是验证 IPC 通信链路畅通。
所有 Command 的入参和返回值必须实现 Serialize 和 Deserialize。
所有 Command 的错误处理统一使用自定义错误类型，不要直接返回 String 类型的错误。
配置文件路径必须通过 Tauri 的 app.path().app_data_dir() 获取，禁止硬编码路径。
六、重要约束与注意事项

6.1 TypeScript 严格模式
tsconfig.json 必须开启 strict: true，不允许使用 any 类型（除非在 Tauri IPC 调用的边界处且标注了 // eslint-disable-next-line）。
所有 Rust 返回给前端的数据结构，前端必须有对应的 TypeScript 类型定义，且与 Rust 的 struct 保持字段名和类型一致。

6.2 组件命名规范
Vue 组件文件名使用 PascalCase：NoteList.vue、WorkspaceDetail.vue
路由对应的页面组件统一放在 src/views/ 下，按模块分子目录：src/views/user/、src/views/workspace/ 等
可复用的通用组件放在 src/components/ 下，按用途分子目录：src/components/common/、src/components/editor/ 等
本阶段不需要创建任何通用组件，除非侧边栏本身需要拆分子组件
6.3 样式方案
考虑后续主题变更，字体大小、颜色等样式变量需要集中定义在一个变量文件中，为后续暗色模式切换预留 data-theme 属性选择器。
使用纯 CSS 或 SCSS，不要在本阶段引入 Tailwind CSS 或其他原子化 CSS 框架。后续如果需要可以再引入。
全局样式变量（颜色、字体、间距等）集中定义在一个 variables.css 文件中，为后续暗色模式切换预留 data-theme 属性选择器。
组件样式使用 <style scoped>，禁止污染全局。


6.4 窗口行为
默认窗口大小：1200 x 800，最小窗口大小：900 x 600
禁止在框架阶段实现自定义标题栏（使用系统默认标题栏），后续如需实现无边框窗口是独立任务
窗口关闭时最小化到系统托盘（如果 Tauri 2.x 当前版本支持且稳定的话），否则正常关闭
6.5 不要做的事
以下是 AI Agent 在本阶段绝对不要做的事情：

不要引入富文本编辑器（如 TipTap、ProseMirror 等），这是后续核心业务任务
不要引入数据库（如 SQLite、IndexedDB 等），本阶段配置持久化用 JSON 文件即可
不要实现拖拽排序，这是后续交互优化任务
不要实现搜索功能，这是后续业务任务
不要写单元测试或 E2E 测试，测试框架的引入是独立任务
不要引入图表库，即使日程模块可能需要日历视图，本阶段用占位文字代替
不要实现任何真实的数据 CRUD，所有列表页用硬编码的模拟数据即可
6.6 必须做的事
以下是 AI Agent 在本阶段必须完成的事情：

✅ 侧边栏可正常渲染，菜单项可点击切换路由
✅ 侧边栏支持折叠/展开，折叠后显示图标
✅ 六个模块的所有页面都有对应的组件和路由，访问后能看到标识性的占位内容
✅ 国际化切换可用，侧边栏菜单文案和页面占位文案都能随语言切换 
✅ Rust 后端模块全部创建，至少有一个示例 Command 能被前端成功调用并返回数据
✅ 统一错误类型在 Rust 和前端都已定义，且能正常传递
✅ 配置文件能在应用数据目录下正常创建和读取
✅ 暗色/亮色模式的全局 CSS 变量已定义，可通过切换 data-theme 属性看到效果（不需要在设置页做切换开关，用开发者工具手动切换验证即可）
✅ package.json 和 Cargo.toml 中的依赖都是本阶段确实需要的，没有多余依赖
七、验证标准
框架搭建完成后，用以下步骤验证是否合格：

pnpm tauri dev 能正常启动，无控制台错误
侧边栏显示 6 个菜单项，文案根据系统语言显示中文或英文
依次点击每个菜单项，右侧内容区显示对应页面的占位内容，URL 正确变化
点击侧边栏折叠按钮，侧边栏收缩为图标模式，再次点击恢复
在设置中切换语言，所有菜单文案立即变为对应语言
关闭应用后重新打开，语言设置保持不变（从配置文件读取）
在浏览器开发者工具中查看，无 TypeScript 类型错误，无 ESLint 警告
查看 Cargo.toml，没有多余的依赖
查看 package.json，没有多余的依赖
